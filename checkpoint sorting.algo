FUNCTION insert_sort(arr : ARRAY_OF INTEGER) : INTEGER{
  var i,j,X;
    for(i=1;i<arr.length;i++)
    X=arr[i]
     j=i-1;
    while (j>=0 && arr[j]>X)
    arr[j+1]=arr[j];
    j--;
    End while;
    End For;
    arr[j+1]=X;
    return arr;
