FUNCTION insert_sort(arr : ARRAY_OF INTEGER) : INTEGER{
  var i,j,srt;
    for(i=1;i<arr.length;i++)
    srt=arr[i]
     j=i-1;
    while (j>=0 && arr[j]>srt)
    arr[j+1]=arr[j];
    j--;
    arr[j+1]=srt;
    return arr;

}
